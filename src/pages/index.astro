---
// TEMPORARY HOLDING HOMEPAGE — in use while the "it starts with one line"
// hero is finalised with real hand lettering. When the lettering is ready it
// replaces the .drawn <span>.
//
// Deliberately bare: no site Header/Footer, no BaseLayout. This page carries
// its own <head> (title, description, canonical, favicon, OG, Person JSON-LD)
// and its own styles, so it depends on nothing but src/config.ts.
// The nav is the shared <Header /> component, wearing the same dark token
// values that /tinkering/ sets — so the menu here is identical to the rest of
// the site by construction, not by copied CSS. Only the hero/footer styling
// below is local to this page.
// Fonts are the self-hosted @fontsource-variable faces — no CDN.
//
// Two earlier versions are parked outside src/pages/, so neither is a route:
//   src/_index.original.astro       the full homepage (selected work + blog)
//   src/_index.holding-chrome.astro this holding page WITH the site chrome
// To restore either one, copy it over this file.
import '@fontsource-variable/fraunces/opsz.css';
import '@fontsource-variable/newsreader/opsz.css';
import { SITE, SOCIALS } from '../config';
import Header from '../components/Header.astro';

const canonical = new URL(Astro.url.pathname, SITE.url).href;
const ogImage = new URL(SITE.defaultOgImage, SITE.url).href;
const sameAs = SOCIALS.map((s) => s.url).filter((u) => u && u !== '#');
const personLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: SITE.author,
  url: SITE.url,
  jobTitle: SITE.jobTitle,
  description: SITE.description,
  image: ogImage,
  ...(sameAs.length ? { sameAs } : {}),
};
---
<!doctype html>
<html lang={SITE.locale}>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#14161d" />
  <title>{SITE.title}</title>
  <meta name="description" content={SITE.description} />
  <link rel="canonical" href={canonical} />
  <link rel="icon" type="image/png" href="/favicon.png" sizes="512x512" />
  <link rel="icon" href="/favicon.ico" sizes="16x16 32x32 48x48" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
  <meta property="og:site_name" content={SITE.name} />
  <meta property="og:type" content="website" />
  <meta property="og:title" content={SITE.title} />
  <meta property="og:description" content={SITE.description} />
  <meta property="og:url" content={canonical} />
  <meta property="og:image" content={ogImage} />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:image" content={ogImage} />
  <script type="application/ld+json" set:html={JSON.stringify(personLd)} />
</head>
<body>
  <Header />

  <main>
    <div class="hero">
      <h1 class="holding-line" aria-label="it starts with one line">
        it starts with <span class="drawn" tabindex="0">one&nbsp;line</span>
      </h1>
      <p class="standfirst">
        Book design &amp; hand lettering for independent authors and small
        publishers — one condensed line, set like a stone.
      </p>
    </div>
  </main>

  <footer>
    <span>© 2026 Vincent Vi</span>
    <nav class="social" aria-label="Social">
      {SOCIALS.map((s) => <a href={s.url}>{s.label}</a>)}
    </nav>
  </footer>
</body>
</html>

<style is:global>
  :root{
    --paper:#fbfbf8;
    --navy:#14161d;
    --green:#a7f175;          /* spring green — legible on dark */
    --green-hi:#eaffd2;       /* foil highlight crest */
    --hairline:#262b38;
    --font-display:'Fraunces Variable',Georgia,serif;
    --font-body:'Newsreader Variable',Georgia,serif;

    /* Tokens the shared Header component reads. Values lifted from the dark
       theme /tinkering/ declares on html:root, so the menu renders pixel-for-
       pixel the same here. Note: on this page --paper is used as a FOREGROUND
       colour (the inverse of its meaning in global.css); the Header does not
       touch it, so it is left alone. */
    --ink:#e6e4dc;
    --ink-muted:#9b9a90;
    --foil:#6f96c4;
    --font-utility:ui-monospace,'SF Mono',Menlo,monospace;
    --gutter:clamp(1.25rem,4vw,3rem);
  }
  *{box-sizing:border-box;margin:0;padding:0;}
  html,body{height:100%;}
  body{
    background:var(--navy);
    color:var(--paper);
    font-family:var(--font-body);
    min-height:100svh;
    display:grid;grid-template-rows:auto 1fr auto;
    -webkit-font-smoothing:antialiased;
    text-rendering:optimizeLegibility;
  }
  a{color:inherit;text-decoration:none;}

  /* The site header is the shared <Header /> component and carries its own
     scoped styles — nothing for this page to declare. The former hand-rolled
     header block (lowercase serif nav, green underline sweep) lived here and
     was removed when the component took over. */

  main{display:grid;place-items:center;padding:2rem clamp(1.25rem,5vw,4rem);}
  .hero{max-width:20ch;text-align:center;}
  .holding-line{
    font-family:var(--font-display);
    font-variation-settings:'opsz' 144,'wght' 400;
    font-weight:400;
    font-size:clamp(2.75rem,9vw,6rem);
    line-height:1.02;letter-spacing:-.015em;
    color:var(--paper);
  }
  .drawn{
    white-space:nowrap;
    color:var(--green);
    background:linear-gradient(100deg,
      var(--green) 0%,var(--green) 42%,
      var(--green-hi) 50%,
      var(--green) 58%,var(--green) 100%);
    background-size:250% 100%;
    background-position:120% 0;
    -webkit-background-clip:text;background-clip:text;
    -webkit-text-fill-color:transparent;
    animation:foil 4.5s cubic-bezier(.45,.05,.35,1) infinite;
    font-variation-settings:'opsz' 144,'wght' 400;
    transition:font-variation-settings .35s cubic-bezier(.45,.05,.35,1);
  }
  @keyframes foil{
    0%,15%{background-position:120% 0;}
    55%,100%{background-position:-20% 0;}
  }
  .drawn:hover,.drawn.tapped{font-variation-settings:'opsz' 144,'wght' 560;}

  .standfirst{
    margin-top:clamp(1.75rem,4vw,2.5rem);
    font-family:var(--font-body);
    font-size:clamp(1rem,2.4vw,1.15rem);line-height:1.5;
    color:rgba(251,251,248,.62);
  }

  footer{
    display:flex;align-items:baseline;justify-content:space-between;gap:1rem;
    padding:clamp(1.25rem,3vw,2rem) clamp(1.25rem,5vw,4rem);
    font-size:.75rem;letter-spacing:.03em;
    color:rgba(251,251,248,.48);
    border-top:1px solid var(--hairline);
  }
  footer .social{display:flex;gap:1.25rem;}

  @media (max-width:560px){
    footer{flex-direction:column;gap:.75rem;align-items:flex-start;}
  }
  @media (prefers-reduced-motion:reduce){
    .drawn{animation:none;background-position:0 0;}
  }
</style>

<script>
  // Mobile tap mirrors desktop hover: momentary weight bump.
  const w = document.querySelector('.drawn');
  if (w) {
    w.addEventListener('pointerdown', () => {
      w.classList.add('tapped');
      setTimeout(() => w.classList.remove('tapped'), 450);
    });
  }
</script>
